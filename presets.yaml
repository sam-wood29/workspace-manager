nuke:
  nuke: true

clear:
  close_others: true

deep_work:
  nuke_first: true
  laptop_fallback: laptop
  open:
    Wispr Flow:
      minimize: true
    Cursor:
      screen: Dell
    Terminal:
      screen: Dell
    Obsidian:
      screen: HP
      obsidian_workspace: workspaces/obsidian_deep_work.json

laptop:
  close_others: true
  open:
    Wispr Flow:
      minimize: true
    Terminal:
      screen: built-in
    Obsidian:
      screen: built-in
      obsidian_workspace: workspaces/obsidian_deep_work.json
    Cursor:
      screen: built-in
